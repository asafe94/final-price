1 - There must have three buttons (one for each business) that changes the Store variable
2 - There must have an input to feed the calculation
3 - There must have a calculate button. This button uses inputvalue and Store variable to generate the text displayed as <p>